<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#1A1A1A"
    android:orientation="vertical">

    <!-- Camera preview container — height is set in code to match native aspect ratio -->
    <FrameLayout
        android:id="@+id/preview_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#000000">

        <TextureView
            android:id="@+id/texture_preview"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

        <!-- Switch-camera button overlaid on preview, top-right -->
        <Button
            android:id="@+id/button_switch_camera"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="top|end"
            android:layout_margin="8dp"
            android:backgroundTint="#88000000"
            android:text="⇄ Front"
            android:textColor="@android:color/white"
            android:textSize="12sp" />

    </FrameLayout>

    <!-- Controls below the preview -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="16dp">

        <TextView
            android:id="@+id/text_video_status"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="16dp"
            android:textColor="#FF4444"
            android:textSize="14sp"
            android:textStyle="bold" />

        <Button
            android:id="@+id/button_video_ptt"
            android:layout_width="match_parent"
            android:layout_height="80dp"
            android:text="Hold to Send Video" />

    </LinearLayout>

</LinearLayout>
